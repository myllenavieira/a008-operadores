const numero = + prompt("Insira um número par");
resto = numero % 2;
console.log(resto);

// Quando executado com números pares o resto da divisão sempre será zero, 
// já que todo número par divido por dois será zero
// Já se inserirmos um número ímpar, o resto será diferente de zero.